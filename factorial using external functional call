#include<stdio.h>
int main()
long factorial(int n);
{
  int n;
  scanf("%d",&n);
  printf("%d!=%ld",n,factorial(n));
  return 0;
}
 long factorial(int n)
{
 int x,fact=1;
 for(x=1;x<=n;x++)
 fact=fact*x;
 return fact;
 }
